/*!
 * Librerias genericas de uso del proyecto
 */
function cargar_enlace(ruta) {
	// aplicar metodo prop para detectar atributos de un objeto dom html
	$("#data-enlace-eliminar").prop("href", ruta);
}
